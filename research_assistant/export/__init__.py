"""Report export — everything that turns a finished task into a file.

`reporting` renders md/docx/pdf (and dispatches tex/paper); `latex` holds the
LaTeX/APA paper emitter. Grouped here so the export surface is one package,
not loose modules at the project root.
"""
